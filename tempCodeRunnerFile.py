  # detected_labels = results.names
        # labels = results.xyxy[0][:, 1].cpu,numpy()

        # for label in labels:
        #     if detected_labels[int(label) == 'person']:
        #         logging.info("person detected")